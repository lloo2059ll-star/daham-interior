name: Kakao link check
on:
  push:
    branches:
      - feature/kakao-link-20260904
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node --test tests/website-ui.test.js
